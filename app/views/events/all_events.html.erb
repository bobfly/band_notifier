<div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 pull-left" style="margin-bottom:15px;">
	
</div>
<br>
<div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
<table class="table">
	<thead class="thead-inverse">
	<tr>
		<th>Datum</th>
		<th>Naziv</th>
		<th class="text-xs-center">Javni nastup?</th>
		<th></th>
		<th class="hidden-md-down">Dogovorio</th>
	</tr>
</thead>
	<tbody>
		<% @events.each do |event| %>
		<tr>
			<td><%= event.event_date.strftime("%d.%m.%Y") %></td>
			<td><%= event.name %></td>
			<td class="text-xs-center"><%= event.public? ? "Da" : "Ne" %></td>
			<td>
				<%= link_to "Uredi", edit_event_path(event.id), class: "btn btn-sm btn-warning" %>
				<%= link_to "Prikaži", event_path(event.id), class: "btn btn-sm btn-info" %>
			</td>
			<td class="hidden-md-down"></td>
		</tr>
		<% end %>
	</tbody>
</table>
</div>